{% extends 'base.html.twig'%}

{% block title%} Accueil{% endblock%}



{%block body%}
<h1>Salut tout le monde</h1>
    <h2>
        {{var}}
    </h2>
    <ul>
        {% if bool%}
            {% for items in tabs %}
              <li>{{items}}</li>
            {% endfor %}
        {% endif%}
    </ul>
{%endblock%}